

<h3>Project definer</h3>

<p>Enter a project and then add your own volunteer by clicking the project and typing the volunteer you wish to add</p>
<br>
<p>Your current projects are:</p>
<% if @projects.any? %>
<ul>
  <% @projects.each do |project| %>
  <li><a href="/projects/<%= project.id %>"><%= project.title %></a></li>
  <% end %>
</ul>
<% else %>
<p>There are currently no projects to display.</p>
<% end %>

<a href="/projects/new">Add a new project</a>
